
name: pynsour
version: 1

# Connections
connections:
- name: example.org
  hostname: irc.example.org
  port: 6667
  username: joeuser
  realname: "Joe User"
  nicks:
  - pybot1
  - pybot2

# Channels are formed in: efnet.org/#pynsour
handlers:
- channel: (.*)/"#(.*)"
  script: \2.py
